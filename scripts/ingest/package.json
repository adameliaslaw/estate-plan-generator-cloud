{
  "name": "estate-plan-ingest",
  "version": "2.0.0",
  "private": true,
  "description": "Bulk ingestion script — uploads PDFs to PageIndex and registers doc IDs in Firestore",
  "scripts": {
    "ingest": "ts-node --esm src/ingest.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-admin": "^13.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
