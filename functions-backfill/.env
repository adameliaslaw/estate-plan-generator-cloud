# Backfill functions use firebase-admin Firestore (gRPC+protobuf) which is memory-heavy.
# Set V8 heap to 4GB within our 8GB container. Without this, Node.js auto-sizes
# the heap too conservatively for a 2GB container, causing heap OOM.
NODE_OPTIONS=--max-old-space-size=4096
