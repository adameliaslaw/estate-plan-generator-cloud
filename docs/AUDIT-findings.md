# Codebase Audit — Findings Ledger

**Mode:** Audit-only (no fixes yet). Fixes batched at the end so we apply encompassing changes, not repetitive per-function patches.
**Scope (round 1):** Document generation pipeline — `functions/src/`.
**Started:** 2026-06-29

Severity key: 🔴 critical · 🟠 high · 🟡 medium · ⚪ low/cosmetic

Status key: `open` · `confirmed` · `wontfix` · `fixed`

---

## ⚖️ RECONCILIATION — 2026-06-29 PM (re-verified against current `main` = 5879857)

**Read this first — it supersedes the per-row statuses below.** This audit was performed against `ce9a466`. While it ran, a **parallel Claude session (`session_01KLDZSGMLWiv6...`) shipped fix PRs #38, #40, #42, #43, #44, #45, #46** to `main` (it never had this ledger). I re-verified the affected findings against current code via 3 verification subagents + diffstat. The ledger body below is the original `ce9a466`-era snapshot; this section is the current truth.

**🔴 Criticals: ALL 9 fixed + deployed green. Zero open criticals.**
- ✅ **AP / AQ / AZ / BA** (+ 🟠 **BB**) — **fixed (#54, 2026-06-29 PM, deployed green):** `createFirmUser` now admin/attorney-only with admin-only admin-minting + role/capability allowlists; `updateUserCapabilities` admin-only + capability allowlist; the broken firm-scope predicate in `listTemplates`/`getTemplateContent`/`searchKnowledgeResources` replaced with `!callerFirmId || callerFirmId !== firmId`. (Remaining B0 items — T6/T8/AR/T9/App Check — are non-critical follow-ups; **AS** fixed in #55.)
- ✅ **DF** — fixed (#40): per-client calendar now queries `clientId`.
- ✅ **AF** — **fixed (#47, 2026-06-29 PM):** `sanitizeObject` no longer re-truncates the canonical `_serializedClientData`/`_clientFullName`/`_spouseFullName` block at 5,000 chars (still injection-stripped; cap lifted to 100k). `sanitizeForPrompt` gained a `{maxLength}` option. Regression test in `tests/unit/ai-client-sanitize.test.ts`. Provider dispatch + serializer schema untouched.
- ✅ **BT** — **fixed (#50, 2026-06-29 PM):** `process-ocr.ts` now runs `stripEmpty()` (drops null/empty-string/empty-array/empty-object) before `set(..., {merge:true})`, so a partial scan can no longer null out existing client data; skips the write entirely when nothing was extracted. Regression test in `tests/unit/process-ocr-strip.test.ts`. (The hardcoded `model:'gpt-5.4'` / T4 routing note is separate and untouched.)
- ✅ **CH** — **fixed (#51, 2026-06-29 PM):** the primary questionnaire `setDoc` now lives inside `performSave`'s retry loop (+ a defensive outer catch), so a failed autosave is retried and surfaced via `SET_ERROR` instead of rejecting silently while the UI shows "Saved". `QuestionnaireContext.tsx`.
- ✅ **BN** — **fixed (#53, 2026-06-29 PM, deployed green):** the LawPay `reference` now carries the payment doc id (`firmId::clientId::paymentDocId`) so `charge.completed` reconciles the pending doc + writes `lawPayTransactionId`. (Scope BN; BO/BP/BQ are B8 follow-ups.)

**✅ FIXED (verified against current code):**
F (#38), S (#40), U (#44), AH *(OpenAI/Anthropic only — see partial)* (#44), T16/register-client (#38), CT (#38/#40), CV (#40), CX (#40), CY (#40), DD (#38), DF (#40), DH (#38), DI (#40), DJ (#40), DL (#46), DY (#38), EN *(the two simple dialogs; ChargePaymentDialog intentionally not)* (#45), EO (#38), the **storage side of AU/T8** (#42 — storage.rules client reads now require `firmId`+`clientId`), and **this session:** AF (#47), BT (#50), CH (#51), **E + A + AE + B** (#52 — honest `success` from status/counts, best-effort bookkeeping, preserve HttpsError codes).

**🔎 IN REVIEW (open PR, not merged):** **BN** (#53) — payment-page reconciliation via doc id in the LawPay reference; held for sign-off (money/IOLTA path, not end-to-end testable without a LawPay sandbox).

**◑ PARTIAL (half fixed, half still open):**
- **CS** — docType strings fixed (#40); the fallback listener still declares success regardless of `status:'error'` → still over-reports. OPEN half.
- **CW** — concurrent-reply clobber fixed via `arrayUnion` (#40); reply `createdAt` is still permanently `null` (can't carry `serverTimestamp()` in an array) → every reply shows "just now". OPEN half.
- **EI / AS** — ✅ fixed: paralegal removed from `canManageUsers` (#43) and from `canManageBilling`/`canManageFirmSettings` (#55, rules + hook, deployed green). A minor `canManageClients` hook-vs-rules mismatch remains (UI-only) — flagged in #55, separate tidy.
- **AH** — truncation now throws in JSON mode for OpenAI/Anthropic; **Gemini & Perplexity still return truncated text silently** (`_callGemini`/`_callPerplexity` ignore finishReason). OPEN half.
- **AU** — storage identity tightened, but **AT** (client can still write `packageDetails.packageType`/fees on their own record) remains OPEN.

**❌ Verified STILL OPEN in changed files (PRs touched the file but not this finding):** H (dead branches), T (OpenAI path still bypasses the long-timeout dispatcher), V (fabricated will/POA metadata on truncation recovery), AO (un-sanitized `firmData`), CR (SingleDoc success ignores `status:'error'`), CU (ESign shows "Sent" without checking `res.success` — only added a pre-call email check), CZ (editor `setContent` still unsanitized), DA (dead regenerate action), DK/DP/DQ/DR (bulk-import: swallowed catch / invalid package / no email validation / dead results map), DM (row-vs-summary amount mismatch), DZ (PaymentsPage `limit(500)` totals), DT (no "in cents" comment), AT (broad client self-update).

**Everything else in the ledger body is in an untouched file → still OPEN** (rounds 1-3: C, D, G, I, J–R, W, X–AB, AC, AG, AI–AN, AR, AV, AW–AY, BB–BM, BO–BS, BU–CG; round 4: CI, CJ, CK, CL, CM, CN, CO, CP, CQ, DB, DC, DE, DG, DN, DO, DS, DU, DV, DW, DX, EA, EB, EC, ED, EE, EF, EG, EH, EJ, EK, EL, EM, EP, EQ, ER, ES, ET).

**Bonus — the parallel session also caught real bugs this audit MISSED (now fixed):** `useFirmBranding` global cache leaking one firm's branding incl. **Maps API key** cross-firm (#38 — a genuine cross-tenant leak my Round 4 missed), FlexDocumentGenerator wrong docType strings → "Unsupported document type" (#40), Single/Flex post-gen poll using a non-existent composite index → fallback never fired (#40), UpcomingAppointments `limit(5)`-then-filter (#40), PaymentsPage "Total Fees" summing voided/refunded/failed (#40), Gemini grounding JSON-mode 400 (#38), grounded-review fenced-JSON parse (#38), knowledge-base-service result-ordering (#38), UploadDraftDialog `onOpenChange` (#38).

**Net after #47/#50/#51/#52/#53/#54/#55:** of ~126 findings, **~39 fixed + deployed, ~2 partial, the rest open**. **All 9 criticals fixed + deployed green** (AF #47, BT #50, CH #51, E/A/AE/B #52, BN #53, AP/AQ/AZ/BA/BB #54, DF #40) plus 🟠 AS #55. **Zero open criticals; no PRs pending.** Remaining work is all 🟠/🟡/⚪ — top of the list: the rest of B0 (T6 ~15 missing role-checks → shared `assertStaff`, T8 storage IDOR, AR firm keys in-browser, T9 Zod, App Check), then the truth-in-status remainder (CR/CU + open halves of CS/CW), then medium cleanups.

---

## Round 1 result — document generation pipeline (7 units, 41 findings)

Files audited: `generate-single-document.ts`, `generate-documents.ts`, `unified-generator.ts`, `template-engine.ts`, `ai-client.ts`, `client-data-serializer.ts`, `generators/*.ts` (×10).

Count by severity: 🔴 ×2 (E, AF) · 🟠 ×5 (A, F, J, M, plus M/N pair) · 🟡 ×~20 · ⚪ ×~14.

**Recommended fix batches (encompassing, not per-file):**

0. **B0 — Privilege escalation / multi-tenant (🔴 do first; touches Never-Break).** Escalation: AP+AQ (admin-only role/capability grants in `user-management.ts`). Cross-tenant reads: AZ+BA+BB (T7 broken predicate → shared `assertFirmAccess`). Missing role checks: BC–BJ (T6 → shared `assertStaff`). Storage IDOR: BH (T8 → path-prefix validation). Plus AR/BK (firm API keys out of client-readable doc / branding key gate), AS (paralegal helpers in `firestore.rules`), AW (logAccess role gate), T9 (Zod at all boundaries), and turn on App Check. *Deferred by user decision (2026-06-29) — contained while single-firm; revisit before onboarding a 2nd firm or non-admin staff.*
1. **B1 — Input/output integrity in generators (🔴 highest ROI).** AF + AH + AI + AJ + AK. Build one `runStandardGenerator()` helper: read `_serializedClientData`/`_clientFullName` before `sanitizeObject` (fixes the 5000-char truncation), surface `callAI` truncation, add empty-content guard, fall back to `undefined` model. Fixes the worst bug and 9 files at once.
2. **B2 — Error propagation (🔴/🟠, T1).** E + A + B + AE. One convention: save/critical failures throw a typed `HttpsError`; bookkeeping `.update()`s are best-effort; entry wrappers re-throw `HttpsError` and derive `success` from status/counts.
3. **B3 — Provider routing (🟠, T4).** M + N + AJ. Decide the contract for hardcoded models (substitution/enhancement) vs firm provider; thread the firm default everywhere.
4. **B4 — Serializer correctness (🟡).** Y (alt healthcare agent path), Z (spouse-assuming residual default), AB ($NaN).
5. **B5 — Resilience (🟡).** S (retry 5xx/529) + T (OpenAI long-timeout dispatcher).
6. **B6 — Legal-prose safety (🟡, T3).** J (name stop-list) + golden-file snapshot tests for the regex passes before further edits. AG (escape questionnaire HTML + verify PDF export path).
7. **B7 — Cleanup (⚪).** C, D, H, U, V, W, P, Q, R, AC, AL, AM, AN, AO + latent X.
8. **B8 — Money correctness (🔴/🟠, LawPay).** BN (reconcile payment-page payments), BO (remove firm-named default page), BP (authorized≠paid), BQ (trust vs operating / IOLTA), BR/AX (cents unit in audit log).
9. **B9 — Data integrity (🔴/🟠).** BT (OCR null-merge — strip nulls before merge), BV+T10 (persist OAuth-revoked state + UI re-auth banner).
10. **B10 — Integration resilience (🟡, T11).** One shared retry/backoff helper for all external calls (ai-client S/T + calendar/drive/SendGrid/Whisper/AssemblyAI/CourtListener/ATTOM CC/CB); dedupe/idempotency (BW, BX); debounce Drive sync (BY); tz on email dates (BZ).
11. **B11 — Truth-in-status (🟠, T12).** Stop reporting success for stub integrations: esign (BU), Levitate (CD), scheduled reminders (CF). Either implement, disable, or label "simulated."
12. **B12 — Integration data-mapping (🟡).** Levitate address mapping (CD), property-data county field (CE), Whisper size guard (CA).

**Running tally across rounds 1-3:** ~80 findings. 🔴 ×5 (E, AF, AP, AQ, AZ/BA, BN, BT — escalation/data-loss/money). Round 4 (frontend) not yet started.

---

## Systemic themes (candidates for one encompassing fix)

- **T1 — Error handling swallows/mislabels failures.** Findings B, E. Save/bookkeeping failures get reported as success or flattened to a generic `internal` code. Candidate for a single error-propagation convention across the pipeline entry points + orchestrator.
- **T2 — Duplicated logic that can drift.** Findings G, H. Spouse-swap rules and doc-type metadata duplicated; redundant defaults/ternaries.
- **T3 — Heuristic regex rewriting of attorney-reviewed legal prose.** Finding L (+J, O). `template-engine.ts` runs ~15 regex/string passes that mutate rendered legal text (normalizeTestatorTitle, normalizeSpouseTitles, repairAiArtifacts, cleanEmptyListSlots, insertOxfordAnd, uppercaseKnownNames, typographyCleanup…) plus an AI "smooth the prose" pass. Each is individually defensible but collectively they're a fragile, hard-to-test corruption surface operating on legal documents. Worth a consolidated test harness (golden-file snapshots) rather than per-pass patches.
- **T4 — Hardcoded / literal-fallback AI models bypass per-firm provider routing.** Findings M, N, AJ. Two paths hardcode a model; all 9 generators use `model: documentDraftingModel || 'gpt-5.4'` whose literal fallback forces OpenAI when a firm picks another provider but leaves `documentDraftingModel` unset. Resolve once against the provider-dispatch contract (fall back to `undefined`, let `callAI` pick the provider default).
- **T5 — Silent data/output loss in the AI generators (no truncation signal).** Findings AF (🔴 input truncated to 5000 chars), AH (output-truncation flag is dead code), AI (no empty-content guard). The generators can silently lose both **input** (canonical data block capped) and **output** (response cut at maxTokens, saved as a clean draft). One shared `runStandardGenerator()` helper (finding AK) would fix AF/AH/AI/AJ in a single place.
- **T6 — Missing role checks on ~15 callables → within-firm client escalation. ✅ FIXED (#56 + #57, deployed green).** Root: `link-client.ts:112` mints `role:'client'`+`firmId`+`clientId`, so a client *passes* firm-scoping and reaches staff-only ops that only check `firmId`. Fix: shared `functions/src/auth-guards.ts` (`assertStaff`/`assertFirmStaff`) applied to all staff-only callables (v2 via `assertStaff`, v1 via equivalent inline role check) — 18 in #56 (connectGoogleDrive, exchangeGoogleAuthCode, chatAi, listAiConversations, getDocumentVersions/getDocumentVersionContent, summarizeTranscription, checkDocumentCompliance, groundedReviewDocument, generateEstateDocument, saveMessageAsNote, enhanceTemplate, calendar push/pull, triggerFirmCalendarSync, sendForSignature, lookupPropertyData, estimateGenerationCost) + the 6 email senders & testSendGridConnection in #57 (**BJ**). `sendQuestionnaireCompleteNotification` intentionally left un-gated (client-triggered on questionnaire submit). **Residual (reclassified to T9):** server-resolve email recipients + HTML-escape interpolated fields.
- **T7 — Broken firm-scope predicate (cross-tenant read).** `if (callerFirmId && callerFirmId !== firmId && role !== 'admin')` — a no-`firmId`-claim caller skips the throw, and any firm `admin` reads cross-firm. In `getTemplateContent`, `searchKnowledgeResources`, `listTemplates`. Fix: unconditional `callerFirmId === firmId` via a shared `assertFirmAccess` helper.
- **T8 — Raw storage paths in admin-SDK downloads (cross-tenant file read). ✅ FIXED (#58, deployed).** The admin SDK bypasses Storage rules, so functions must validate path prefixes themselves; `bulkProcessKnowledgeFiles`, `processQuestionnaireScan`, `transcribeAudio` used caller-supplied `storagePath`/`imagePaths` raw. `processTemplateFile` was the correct template (validates prefix). Fix applied to all three: require `path.startsWith('firms/{firmId}/')` + reject `..`. Guard inlined per call site (not via `auth-guards.ts`) because two are v1 callables where a v2 `HttpsError` would be re-wrapped as `internal`.
- **T9 — Missing Zod / unbounded inputs into AI + email.** Cost/DoS and content-injection surface: chatAi, enhanceTemplate, generateFlexDocument, saveMessageAsNote, createFirmUser/updateUserCapabilities, plus raw user strings interpolated into firm-branded email HTML. Fix: Zod schemas with length caps at every callable boundary; server-resolve email recipients; sanitize interpolated fields.
- **T10 — Revoked OAuth (`invalid_grant`) never persists a disconnected state.** Calendar (`calendar-sync.ts:304-316`) and Drive (`google-drive-sync.ts:114-138`) throw on revoke; the scheduled/triggered job swallows it and retries forever — the firm is never told to re-auth. This is the code-level root of the recurring Calendar `invalid_grant` in memory [[project_google_oauth_refresh_token_expiry]]: even after the consent-screen fix, nothing surfaces re-auth need. Fix: on `invalid_grant`, write `connected:false`/`authError` and show it in the UI.
- **T11 — No transient-retry/backoff on any external call.** ai-client (S), calendar, drive, SendGrid, Whisper, AssemblyAI poll, CourtListener, ATTOM all treat the first 429/5xx as terminal (some abort mid-job/mid-pagination). One shared backoff helper hardens all of them.
- **T12 — Stub integrations report success.** esign returns `{success:true}` + writes "sent for signature" with no send (BU); Levitate's API-key path posts to a self-described mock endpoint (CD); email `scheduledReminders` is commented out so the advertised 24-hour reminder never fires (CF). Each tells staff/clients a legal/CRM action happened that didn't.

---

## Round 2 — security & access (in progress)

> ⚠️ **CRITICAL — privilege escalation + cross-tenant breach. Recommend fixing AP/AQ before anything else, audit-only mode notwithstanding.**

### `user-management.ts` + `firestore.rules`

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| AP | 🔴 | **fixed (#54)** | **Any staff can mint an `admin` → cross-tenant breach.** `createFirmUser` authorizes the caller if role ∈ {admin,attorney,paralegal} (L72), then sets the new user's custom claims to the **request-supplied `role`** (L88) with no admin-only gate. A paralegal/attorney creates `role:'admin'` for an email they control. Because `firestore.rules isAdmin()` bypasses `belongsToFirm()` on every collection, that admin reads **every firm's** client data. Fix: only `admin` may grant `admin`; gate assignable roles by caller role; Zod-validate `role`. |
| AQ | 🔴 | **fixed (#54)** | **Self-service capability escalation.** `updateUserCapabilities` authorizes any staff caller (L209) and writes **arbitrary `capabilities` to any user in the firm, including the caller** (L227, no allowlist). `firestore.rules` honors these via `hasCapability()` (`canManageFirmSettings/Users/Billing`). A paralegal grants themselves `manage_firm_settings` → read/write the firm doc (incl. API keys), `manage_users` → then mint an admin (→ AP). Fix: admin-only; validate capabilities against a fixed allowlist. |
| AR | 🟠 | **fixed (#59, deployed + migrated)** | **Firm API keys exposed to the browser.** Per-firm provider keys lived on `firms/{firmId}` (client-SDK readable by in-firm staff → XSS-exfiltratable). Fixed: keys moved to Functions-only `firms/{firmId}/secrets/apiKeys` (`allow read, write: if false`); backend merges them via `loadFirmSecrets()` at ~13 load sites; new `updateFirmApiKeys` callable saves keys server-side + writes masked `{field}Set`/`{field}Last4` indicators; `migrateFirmApiKeysToSecrets` (admin) moved elias-counsel's 9 keys & deleted the raw copies (verified: 0 raw keys remain on firm doc). `lawPayPublicKey` kept (publishable, browser-read by ChargePaymentDialog). |
| AS | 🟠 | **fixed (#43+#55)** | **Paralegals over-permissioned in `firestore.rules`.** `canManageFirmSettings()` (L62), `canManageUsers()` (L66), `canManageBilling()` (L78) all include `hasRole('paralegal')`, contradicting the documented paralegal scope ("write only notes, calendar, documents"). Concretely paralegals can update the firm doc (L219), create/delete/role-change users (L244/248), and write payments (L444-454). Tighten helpers to the intended matrix. |
| AT | 🟡 | open | **Broad client self-update.** Client self-update validates only `personalInfo` + `status` + identity-unchanged (L342-351); every *other* field on their own record is writable — including `packageDetails.packageType`. If package drives billing/scope, a client can self-upgrade (e.g. to `fortress`). Add field-level restrictions on client writes. |
| AU | 🟡 | open | **Storage vs Firestore client-identity mismatch.** `storage.rules` gates client file access on a `token.clientId` claim (L15,29,42), but `firestore.rules` uses `uid`/`linkedUserId` (`isOwnClientRecord`). A linked/anonymous client session without a `clientId` claim can read their Firestore record but **not** their own audio/scans/documents in Storage (and vice-versa). Align the identity check. |
| AV | ⚪ | open | `createFirmUser`/`updateUserCapabilities` lack Zod validation (raw `as` casts, manual null checks); `role` type includes `'staff'` (L19) which `firestore.rules` doesn't recognize. Branding files are world-readable across firms (`storage.rules` L66, `if request.auth != null`) — low sensitivity but cross-tenant. |

### `audit-trail.ts`

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| AW | 🟡 | confirmed | **`logAccess` has no role gate.** It checks auth + firm match (L140-156) but **not role**, so any authenticated firm user — including `role:'client'` — can write to the compliance audit trail with attacker-controlled `clientName`/`action`/`clientId` (free-text, unsanitized → forged entries + log injection). Restrict to staff roles, validate `clientId` ownership, and sanitize free-text. |
| AX | 🟡 | confirmed | **Audit log misreports payment amounts.** `onPaymentCreated` guesses cents-vs-dollars with `rawAmount > 500` (L296). A $600 payment stored in dollars renders as **$6.00**; a $400 payment stored in cents renders as **$400**. Store an explicit unit instead of guessing. (Symptom of a broader amount-unit ambiguity — see payments.) |
| AY | ⚪ | confirmed | `logAuditEvent` silently swallows write failures (L104-112) — intentional (don't crash caller), but compliance events can vanish with no alert/retry. Consider a dead-letter/metric. |

### Callable/HTTP security sweep (~70 endpoints; subagent + spot-verified)

Tenant isolation is broadly solid — firm-scoping is present almost everywhere; these are the exceptions. Detail rows below; see themes **T6/T7/T8/T9**.

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| AZ | 🔴 | **fixed (#54)** | **`getTemplateContent` cross-tenant template read.** Broken predicate `seed-templates.ts:270` (verified) → no-firmId caller or any firm `admin` reads `templateCollection(firmId).doc(templateId)` for *another* firm; returns full `snap.data()`. **T7.** |
| BA | 🔴 | **fixed (#54)** | **`searchKnowledgeResources` cross-tenant KB read.** Same broken predicate `knowledge-base.ts:204` → reads up to 200 full KB resources (~50KB each) from any firm. **T7.** |
| BB | 🟠 | **fixed (#54)** | **`listTemplates` cross-tenant template listing** (`seed-templates.ts:212`, same predicate) — metadata + 200-char preview + variables of another firm. **T7.** |
| BC | 🟠 | **fixed (#56)** | **`connectGoogleDrive` (`google-drive-sync.ts:376`) no role gate.** A client overwrites `firms/{firmId}.googleDrive` with their **own** refresh token; the sync trigger then uploads every firm client doc to the attacker's Drive. Highest-impact of the missing-role-check set. **T6.** |
| BD | 🟠 | **fixed (#56)** | **`exchangeGoogleAuthCode` (`google-auth.ts:14`) no role gate** — client swaps firm calendar OAuth tokens for their own. **T6.** |
| BE | 🟠 | **fixed (#56)** | **`chatAi` (`chat-ai.ts:477`) no role gate + IDOR** — client passes any in-firm `clientId`; draft mode returns that client's full aggregated estate data. Also unbounded `message` → AI cost/DoS. **T6/T9.** |
| BF | 🟠 | **fixed (#56)** | **`listAiConversations` (`chat-ai.ts:905`)** — no role gate, `clientId` optional → client omits it and reads all firm AI conversations. **T6.** |
| BG | 🟠 | **fixed (#56)** | **`getDocumentVersionContent`/`getDocumentVersions` (`document-versions.ts:61/17`)** — no role gate; client reads full content / history + preview of any in-firm document (admin SDK bypasses Firestore rules). **T6.** |
| BH | 🟠 | **fixed (#58)** | **`bulkProcessKnowledgeFiles`/`processQuestionnaireScan`/`transcribeAudio` storage IDOR** (`bulk-knowledge-import.ts:269`, `process-ocr.ts:12`, `transcribe-audio.ts:104`) — caller-supplied path downloaded via admin SDK without prefix check → cross-firm file read/OCR/transcription (transcribeAudio is staff-gated, so staff-only there). Fixed: each now requires `path.startsWith('firms/{firmId}/')` + rejects `..`, matching `processTemplateFile`. **T8.** |
| BI | 🟡 | **fixed (#56)** | **`summarizeTranscription`, `checkDocumentCompliance`, `groundedReviewDocument`, `generateEstateDocument`, `saveMessageAsNote`, `enhanceTemplate`, `pushEventToGoogleCalendar`/`pullGoogleCalendarEvents`, `sendForSignature`** — all firm-scoped but no role gate → client reaches staff-only operations (AI quota burn, doc mutation, IDOR writes). **T6.** |
| BJ | 🟡 | **mostly fixed (#57)** | **7× `email-notifications.ts` senders** (L319/426/517/627/750/854/1009) — no role gate + caller-supplied recipient + raw string interpolation → any firm user sends firm-branded mail (firm SendGrid sender) to arbitrary addresses, with HTML/content injection. Resolve recipient server-side; gate to staff; sanitize. **T6/T9.** |
| BK | 🟡 | confirmed | **`getFirmBranding` (`branding.ts:13`)** returns `settings.googleMapsApiKey` to any authenticated caller for any `firmId` (only mitigation: HTTP-referrer restriction). Gate the key on `firmId` match. *(Related to AR.)* |
| BL | 🟡 | confirmed | **`linkClient` (`link-client.ts:32,97`)** matches client records by `auth.token.email` **without** `email_verified` → a user with an unverified matching email claims an unlinked client record and reads its estate data. Require `email_verified === true`. |
| BM | ⚪ | **partly fixed (#56)** | Lower-risk: `triggerFirmCalendarSync`/`lookupPropertyData`/`estimateGenerationCost` (client can burn firm API quota), `linkClient` prospect-create abuse, `registerClientFromLink` (public; add App Check/rate-limit), `willsDriveWebhook` non-standard channel-token model, and various unbounded-input/no-Zod staff-gated write endpoints (**T9**). See subagent table for file:line list. |

**Verified SAFE** (auth+role+firm-scope correct): generateDocuments, generateSingleDocument, reviewDocument, processTemplateFile, export*(pdf/docx/batch), createPaymentRequest, processDirectCharge, lawpayWebhook (HMAC + timingSafeEqual), all KB CRUD, uploadTemplate/deleteTemplate, revertDocumentVersion, and all Firestore/scheduled triggers. **Not fully traced** (recommend manual): App Check (`enforceAppCheck` is set on *no* endpoint), `ai-memory.ts#listConversations`, `chatAi` mid-body writes, and the `clients` subcollection-vs-top-level data-model question.

## Round 3 — integrations reliability (in progress)

### `lawpay-integration.ts` (money path — audited directly)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| BN | 🔴 | **fixed (#53)** | **Payment-page payments are never reconciled.** `createPaymentRequest` saves the Payment doc with an **auto-ID** (L320) and never writes `lawPayTransactionId`. The webhook's fast path builds `paymentRef(firmId, clientId, transactionId)` (L571) — a doc that doesn't exist (id ≠ transactionId) → `snap.exists` false → no-op; and the fallback queries `lawPayTransactionId == transactionId` (L578) which was never set → empty. So `charge.completed` for the entire payment-page flow silently does nothing → **client pays, app still shows "pending" forever.** Fix: store the doc under a deterministic id and write `lawPayTransactionId` on create, or resolve by the `reference` you actually set. |
| BO | 🟠 | confirmed | **Hardcoded default payment page → money misrouting.** `LAWPAY_PAYMENT_PAGE_URL = 'https://secure.lawpay.com/pages/bolsterbrudereliasllc/operating'` (L222). Any firm without `lawpayPaymentPageUrl` configured routes its clients' payments to **that specific named LawPay account**. In a multi-firm deployment this sends one firm's funds to another's bank. Make it required config; no firm-named default. |
| BP | 🟠 | confirmed | **Authorization treated as settlement.** `processDirectCharge` marks the record `status:'paid'`, `balanceDue:0`, `paidAt:now` on an **AUTHORIZED** charge (comment L872 says auto-capture happens later daily). If the later capture fails/voids, the record wrongly shows paid and — because BN breaks webhook reconciliation — there's no correction path. Mark `authorized` until a capture/settlement webhook confirms. |
| BQ | 🟡 | confirmed | **Trust vs operating not honored (IOLTA/RPC 1.15 risk).** `processDirectCharge` hardcodes `accountDesignation:'operating'` (L854/891) and takes no designation input; `createPaymentRequest` stores the designation but the page URL is fixed to `/operating`. Retainers/trust funds can't be routed to the trust account → client funds land in operating. For a NJ attorney this is a trust-accounting compliance exposure. |
| BR | 🟡 | confirmed | Confirms **AX**: payments are *always* stored in cents here, so `audit-trail`'s `amount > 500` cents-guess misreports every sub-$5 payment (and is ambiguous around the boundary). Replace the heuristic with the known cents unit. |
| BS | ⚪ | open | Cleanups: `createPaymentRequest`/`processDirectCharge` use the **v1** functions API (`firebase-functions/v1`) while the webhook is v2 — mixed API/stale; inline SendGrid HTML duplicates `email-notifications.ts`; `cors` on a server-to-server webhook (L501) is pointless; `rawBody` fallback `JSON.stringify(req.body)` (L520) can break signature verification if `req.rawBody` is absent. |

### Integrations (esign, calendar, drive, email, transcription, OCR, levitate, courtlistener, property — subagent + spot-verified)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| BT | 🔴 | **fixed (#50)** | **OCR overwrites good client data with null.** `process-ocr.ts:124` `clientRef.set(extractedData, {merge:true})` where the model is told to return `null` for unreadable fields → scanning a partial page **nulls out existing** spouse/children/contact/etc. on the client record (the source of every generated document). Strip null/empty before merge, or merge only extracted fields. (Also `model:'gpt-5.4'` hardcoded L98 — T4.) |
| BU | 🟠 | confirmed | **E-sign reports false success.** `esign-service.ts:69` returns `{success:true, signatureRequestId}` and writes a "Document Sent for E-Signature" activity entry after only a `console.log` (L67) — nothing is sent. Attorney believes a will/POA went to the client for signature; it didn't. Throw `unimplemented` or label "simulated" until a provider is wired. **T12.** |
| BV | 🟠 | confirmed | **OAuth revoke → silent-forever sync failure.** Calendar (`calendar-sync.ts:304-316`) + Drive (`google-drive-sync.ts:114-138`) throw on `invalid_grant` but never persist a disconnected/needs-reauth flag; the scheduled job retries every 5 min indefinitely, firm never notified. **T10** / memory [[project_google_oauth_refresh_token_expiry]]. |
| BW | 🟡 | confirmed | Calendar pull dedups against only the first 30 of 50 fetched event IDs (Firestore `in` cap, `calendar-sync.ts:660-673`) → events 31-50 create **duplicate `CalendarEvent` docs** on every pull. Chunk the `in` query. |
| BX | 🟡 | confirmed | Drive `findOrCreateFolder` (`google-drive-sync.ts:156`) interpolates `folderName` into the Drive `q` unescaped → a client named **O'Brien/D'Angelo** breaks the search → duplicate/failed folders. Plus a find-or-create race → duplicate folders. Escape `'`; serialize first creation. |
| BY | 🟡 | confirmed | Drive sync trigger (`google-drive-sync.ts:466`) regenerates a PDF via Puppeteer (2GiB) + uploads on **every** document write (each autosave). No debounce → cost/latency. Trigger only on a "ready" status transition. |
| BZ | 🟡 | confirmed | Email dates use `toLocaleDateString('en-US')` **without `timeZone`** (`email-notifications.ts:467,566,654,788,1226`) → render in server UTC → **off-by-one day** for any activity after ~8pm ET (NJ). Pass `timeZone:'America/New_York'`. |
| CA | 🟡 | confirmed | Whisper's 25 MB limit vs Storage's 50 MB allowance (`transcribe-audio.ts:240`) → 25-50 MB audio always fails opaquely. Pre-check size / chunk. |
| CB | 🟡 | confirmed | AssemblyAI poll (`assemblyai-transcribe.ts:222`) aborts the whole job on a single transient GET error though the job is still processing. Tolerate a few consecutive poll errors. **T11.** |
| CC | 🟡 | confirmed | No retry/backoff on transient 429/5xx across calendar/drive/SendGrid/Whisper/AssemblyAI/CourtListener/ATTOM. **T11.** |
| CD | 🟡 | confirmed | `levitate-sync.ts:31` maps `personalInfo.city/state/zip` but the data lives under `personalInfo.address.{city,state,zipCode}` → those ship empty and `address` ships an **object** (`[object Object]`). Also the API-key path posts to a self-described **mock** endpoint (L60) → always fails for key-only firms. **T12.** |
| CE | 🟡 | confirmed | `property-data.ts:187` reads `county` from `address.countrySubd` — that's the **state** in ATTOM, not county. Deeds/affidavits prefill the wrong county. Map from `countySecSubd`. |
| CF | 🟡 | confirmed | `email-notifications.ts:1119-1181` — `scheduledReminders` is commented out; the advertised 24-hour appointment reminder is never sent. **T12.** |
| CG | ⚪ | open | Lows: esign dead `firmDoc` fetch (L21); calendar falls back to `now` for unparseable event times (writes wrong start/end); transcribe marks empty Whisper output as `completed`; `processCustomTemplate` `$`-replacement bug (email L287); CourtListener/ATTOM no fetch timeout; OCR silently drops missing image paths; Drive `expires_in` `NaN` expiry with no `?? 3600` fallback (L129). |

## Findings

### `generate-single-document.ts`

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| A | 🟠 | **fixed (#52)** | Post-save `updatedAt`/`updatedBy` `.update()` (L131) is inside the same try as generation; a transient failure there makes a *successful* generation report as `internal` failure → attorney retries → duplicate doc. Make it best-effort (own try/catch). Path confirmed correct (subcollection). |
| B | 🟡 | **fixed (#52)** | All errors rewrapped as `'internal'` (L148-151), discarding meaningful `HttpsError` codes thrown deeper (`not-found`, `failed-precondition`). Also masks OOM root cause (cf. memory `project_function_oom_256mib`). Fix: `if (error instanceof HttpsError) throw error;` before generic wrap. Part of **T1**. |
| C | 🟡 | open | Default `generationMode = 'template'` (L81) contradicts orchestrator default `'hybrid'` (unified-generator L427) and CLAUDE.md "hybrid is default". Single-doc regen silently runs a different mode than batch unless caller specifies. Decide intended default. |
| D | ⚪ | open | Log prints `propertyIndex=undefined` when omitted (L107). Cosmetic. |

### `unified-generator.ts`

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| E | 🔴 | **fixed (#52)** | **Save failure reported as success.** `saveDocumentToVault` throw is caught and *returned* as `{status:'error', currentVersion:0}` without re-throwing (L1091-1105); wrapper returns `success:true` regardless (single-doc L138). Attorney believes doc saved; vault has nothing. Re-throw on save failure, or derive `success` from `result.status`. Part of **T1**. |
| F | 🟠 | open | Auto-retry for per-property docs drops the `property` arg (L936 vs original L844). A failed deed/affidavit/gitRep3 retries *without* property data → retry is worse than original. Thread `property` into retry. |
| G | 🟡 | open | Spouse-swap logic duplicated: field-backfill + gender-inversion runs separately for `clientData` (L525-538) and `clientContext` (L623-634); `HOUSEHOLD_REL` declared twice (L547, L679). Drift risk — a rule fixed in one copy, missed in other, makes generator and template engine see different data. Extract shared helper. Part of **T2**. |
| H | 🟡 | open | Dead/redundant: L1075 `finalStatus === 'error' ? 'error' : finalStatus` (both branches identical); L1011/1030/1116 `generationMode ?? 'hybrid'` can't fire (already defaulted L427). Part of **T2**. |
| I | ⚪ | open | `textLength === 0` logs "🚨 EMPTY DOCUMENT" from stripped HTML (L1028); false alarm if any generator returns content only via `_binaryBuffer`. Verify against generators. |

### `template-engine.ts`

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| J | 🟠 | open | **`uppercaseKnownNames` will uppercase + bold common-word names.** `collectKnownNames` (L711) pushes bare first/last/child names; `uppercaseKnownNames` (L529) matches **case-sensitively** with word boundaries. A client/child named **Will, Mark, Grant, Hope, Faith, May, June, Rich, Reed, Read…** → every capitalized occurrence in the legal prose gets uppercased and `<strong>`-wrapped, e.g. "Last **WILL** and Testament." Real, user-visible on the wet-sign doc. Needs a stop-list or restrict bolding to full-name (multi-token) matches only. |
| K | 🟡 | open | Nested Handlebars sub-expression extraction is broken. `extractFromExpression` (L149-158) only strips one level of `(...)`; outer expressions like `(eq (inc a) b)` leave paren-attached tokens (`(eq`, `b)`) that pass `addIfVariable` and get stored as bogus variables → false "missing variable" validation warnings. Extraction-only impact (not rendering). |
| L | 🟡 | open | Systemic — see **T3**. The post-processing regex suite mutates legally-reviewed prose with no golden-file tests. Individual passes look correct but interactions (order-dependent, run on every return path) are untested. Recommend snapshot tests before any further edits here. |
| M | 🟠 | confirmed | **`substituteTemplateValues` hardcodes `model: 'gpt-4o'`** (L2314) for the raw-uploaded-template path. **Confirmed:** `callAI` L175-182 infers provider from model name, so this forces OpenAI regardless of `activeAiProvider`. For an Anthropic-only firm with no OpenAI key (no `process.env.OPENAI_API_KEY`), `_callOpenAI` throws → `substituteTemplateValues` catch (L2356) **returns the raw sample template unsubstituted** (wrong-client data, see O). Also `gpt-4o` stale. Part of **T4**. |
| N | 🟡 | confirmed | **`enhanceWithAI` hardcodes `model: 'claude-haiku-4-5-20251001'`** (L2505). **Confirmed:** forces Anthropic (callAI L178); `_callAnthropic` has **no env-key fallback** (L296). A firm without an Anthropic key → throw → caught (L2520) → returns un-enhanced template. Net: **hybrid mode silently provides zero AI enhancement for non-Anthropic firms.** Part of **T4**. |
| O | 🟡 | open | On substitution fidelity failure (L2336-2345), returns the **raw template with its baked-in sample-client data** + a warning banner. If the attorney misses the banner, a legal doc ships with another person's name/address. Banner-mitigated but high-blast-radius. |
| P | 🟡 | open | `autoFillSpouseInfoAddress` only fires for `maritalStatus === 'married'` (L1700), but `autoFillSpouseFiduciaryAddresses` accepts partner/domestic-partner (L1642). Domestic-partnership clients get blank spouse address on the HC directive while Will/POA fill correctly. Inconsistent household-address logic. |
| Q | ⚪ | open | `renderTemplate` calls `Handlebars.compile` on every generation (L1942); no compile cache. Batch generating N docs from one template recompiles N times. Minor CPU/latency. |
| R | ⚪ | open | `gpt-4o` (L2314) is a stale model id given the current model lineup; revisit when M is addressed. |

### `ai-client.ts` (Never-Break list — provider dispatch)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| S | 🟡 | open | `fetchWithRetry` (L221-234) **only retries HTTP 429**. Transient 5xx — notably Anthropic **529 "overloaded"** (common) and 500/502/503 — are not retried, so a generation fails on a momentary provider blip that a single retry would survive. Add 5xx/529 to the retry condition. |
| T | 🟡 | open | **OpenAI SDK path bypasses the long-timeout dispatcher.** `LONG_REQUEST_AGENT` (10-min undici agent, added precisely because the default 300s `headersTimeout` kills large AI calls) is only passed inside `fetchWithRetry` (Anthropic/Gemini/Perplexity raw fetch). `_callOpenAI` uses the OpenAI SDK (L248) which hits the **default 300s headersTimeout**. The OpenAI path is exactly where big calls run (gpt-4o substitution of ≤50K-char templates, 16K maxTokens) → can time out. Fix: `setGlobalDispatcher(LONG_REQUEST_AGENT)` or pass a configured client `timeout`/`httpAgent`. |
| U | 🟡 | open | Stale/misleading comments on the content-filter fallback (Never-Break path). Comment says "fall back to **Gemini**" (L193) and "Re-throw if … no **Gemini** key" (L209), but the code falls back to **OpenAI** (L201-207). Code matches CLAUDE.md; comments are wrong → future maintainer hazard. |
| V | ⚪ | open | `parseAIJson` truncation recovery path 1 fabricates `metadata: { witnessRequired:false, notarizationRequired:false }` (L822-828) for a recovered **will/POA**, where both are typically **true**. Mitigated by `_truncated:true` → needs_review, but if any downstream logic trusts that metadata it's wrong for legal docs. |
| W | ⚪ | open | Redundant clause: `m.startsWith('gpt-5')` (L180) is already covered by `m.startsWith('gpt')` earlier in the same `||`. |

### `client-data-serializer.ts` (Never-Break list — canonical prompt context)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| X | ⚪ | confirmed-latent | **`maritalStatus` two conventions (latent, not live).** Verified: questionnaire + all frontend write exact-case `'Married'`/`'Domestic Partnership'` (QuestionnaireComplete L222, ClientDashboardPage L412, SingleDocumentGenerator L159, PrintableQuestionnaire L364), so the exact-case checks in serializer (L132) and `isMarriedCouple` (L113) are correct **today**. `template-engine`'s `.toLowerCase()` (L1700/1736) is the lone defensive outlier. No live bug; just two conventions that could diverge. Standardize on normalize-on-read to be safe. |
| Y | 🟡 | confirmed | **Alternate healthcare-proxy dropped from AI prompt.** Verified: questionnaire writes `fiduciaries.healthcareProxy.alternateAgent` (`questionnaire.ts:2620`; typed `index.ts:617`), but serializer reads `healthcare.alternate` (L233). Primary handles both shapes (`primary ?? agent`, L232); alternate does **not** → the alternate healthcare agent is silently absent from the canonical AI context for the advance directive. One-line fix: `healthcare.alternate ?? healthcare.alternateAgent`. |
| Z | 🟡 | open | Default residual distribution assumes a spouse: injects "100% to spouse, if living, otherwise equally to children, per stirpes" (L386) even for single/widowed/divorced clients with no spouse. Misleading default in the AI context. Gate on `hasSpouse`. |
| AA | ⚪ | open | Double sanitization: `sanitizeObject(clientData)` (L116) already recursively sanitizes every string, then dozens of call sites re-run `sanitizeForPrompt` on the same values (L182, 188, 256…). Wasted CPU + double 5000-char truncation. Pick one layer. |
| AB | ⚪ | open | `Number(x).toLocaleString()` on asset values (L258, 272, 285…) yields `"$NaN"` in the prompt if a value was stored as a formatted string (e.g. `"500,000"`). Coerce/strip non-numerics first. |

### `generate-documents.ts` (batch entry)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| AC | 🟡 | open | **Spouse-variant docs can be silent duplicates.** `expandForMarriedCouple` (L126-144) queues a `spouseRole:'spouse'` copy of will/poa/livingWill/pourOverWill for married clients. But the spouse swap in unified-generator only fires when `clientData.spouseInfo` exists (L511). If the couple is married but spouse data was never entered, the swap no-ops and the `_spouse` doc is generated from the **client's** identity → the spouse's will names the client as testator. Skip (or hard-warn) the spouse variant when `spouseInfo` is empty. |
| AE | 🟡 | **fixed (#52)** | Final client `.update()` (L306) is **not** wrapped — throwing after all docs are generated and saved reports the whole batch as failed though the vault is populated (same class as A, **T1**). Also `return { success: true }` (L325) is unconditional even when `documentsErrored === total` (all failed). Derive `success` from counts. |

### `generators/*.ts` (10 document generators)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| AF | 🔴 | **fixed (#47)** | **Canonical CLIENT DATA BLOCK silently truncated to 5000 chars in all 9 AI generators.** Each generator does `sanitizeObject(clientData)` (will L96) then reads `safe._serializedClientData` (L100) — but `sanitizeObject` recurses and re-runs `sanitizeForPrompt`, which hard-caps strings at 5000 chars (ai-client L603-607). The serialized block (CLIENT+SPOUSE+CHILDREN+FIDUCIARIES+ASSETS+DISTRIBUTION+TRUST+HEALTHCARE+SPECIAL+FIRM) exceeds 5000 chars for any non-trivial client, so the tail (FIRM block, special provisions, often distribution detail) is **cut from what the AI sees** — silent legal-content loss on real documents. Also wasteful triple-sanitization (serializer→sanitizeObject→per-field). Fix once: read `_serializedClientData`/`_clientFullName` **before** `sanitizeObject`, or exempt those keys from the cap. **T5.** |
| AG | 🟡 | confirmed | **`questionnaire-generator` performs no HTML escaping.** `val()` does `String(v)` (L23) and interpolates raw client input into HTML (`field` L38, `fiduciaryBlock` L48); the file never imports `sanitizeObject`/`sanitizeForPrompt` — the only generator that bypasses sanitization. In-app preview is DOMPurify-protected (`sanitizeHtml` in DocumentPreviewDialog/VersionHistoryDialog), so stored-XSS-on-render is mitigated, **but** (a) it's a defense-in-depth gap and (b) the **Puppeteer PDF export renders raw HTML** — verify that path. Clients can self-generate this doc, so injected markup reaches an attorney's view. Escape at source regardless. |
| AH | 🟡 | confirmed | **`_truncated` is dead code in every generator.** `...(parsed._truncated && {_truncated:true})` (will L164) can never fire: `DOCUMENT_SCHEMA` is `strict`/`additionalProperties:false` (forbids the field), and `callAI` only *logs* `finish_reason==='length'` (ai-client L281-286), never propagates it. AI responses cut at `maxTokens` (16384, less for some) are saved as clean `status:'draft'` legal docs. Fix in `callAI`: surface truncation out-of-band; honor it in generators. **T5.** |
| AI | 🟡 | confirmed | **Empty-content guard exists only in POA** (poa L166-171). The other 8 return `content: parsed.content ?? ''` with `status:'draft'` (will L162) → an empty AI response becomes a blank "draft" legal document. Promote POA's guard to the shared helper. **T5.** |
| AJ | 🟡 | confirmed | All 9 AI generators: `model: documentDraftingModel || 'gpt-5.4'` (will L150). The literal fallback forces provider=openai (callAI L180) for any firm with `activeAiProvider` set but `documentDraftingModel` unset → throws if no OpenAI key. Masked for elias-counsel (model set to claude-opus-4-8). Fall back to `undefined`. **T4.** |
| AK | ⚪ | open | Heavy copy-paste scaffolding across generators (safe/safeFirm setup, `_clientFullName` fallback chain, preset resolution + `%%FORMATTING_RULES%%` replace, callAI options block) — ~80 dup lines/file. A shared `runStandardGenerator()` would collapse AF/AH/AI/AJ into one place. **T2.** |
| AL | 🟡 | open | `deed-generator` `maxTokens:6144` (L187) risks output truncation on a full recordable deed (legal description + covenants + spousal joinder + per-signatory notary blocks); combined with dead `_truncated` (AH) a cut-off deed saves as a clean draft. Raise to ~8-10k. |
| AM | 🟡 | open | `deed`/`affidavit`/`gitRep3` default `property` to `{}` (deed L108); if the orchestrator omits it, the doc renders with blank address/block-lot and still returns `status:'draft'`. Error when address is empty. |
| AN | ⚪ | open | `pour-over-will-generator` builds the trustee name from `[trustee.name]` only (L127-128); if the trust stores name parts (first/last) rather than a single `name`, the pour-over article's trustee renders blank. Verify trust trustee shape. |
| AO | ⚪ | open | `summary-docs-generator` passes **un-sanitized** `firmData` to `callAI` (L517) while every other generator passes `safeFirm` — harmless for routing, but inconsistent (and skips prompt-injection sanitization of firm fields). |

---

## Round 4 result — frontend (`src/`, 119 non-UI files, 46 findings: CH–ET)

Audited 2026-06-29 via 6 parallel read-only subagents (one per domain): auth/state/services, admin pages, client+questionnaire, documents/editor, dashboard/payments/clients, knowledge/settings/ai/layout/lib. UI primitives (`components/ui/`) out of scope. No code changed.

Count by severity: 🔴 ×2 (DF, CH) · 🟠 ×16 · 🟡 ×~21 · ⚪ ×~7.

**🔴 The 2 frontend criticals:**
- **DF** — per-client calendar queries `where('relatedClientId', ...)`, a field that is **never written** (events save `clientId`; the type has no `relatedClientId`) → every client's CalendarTab is permanently empty, including events just created. One-token fix.
- **CH** — `QuestionnaireContext.performSave` wraps the **primary** client-data `setDoc` in `try…finally` with **no `catch`** (only the secondary `questionnaireProgress` write is retried/`SET_ERROR`'d). Autosave calls it via `void performSave(...)`, so a rejected write (permissions/network) is an unhandled rejection: no retry, no error shown, `isSaving` flips back → UI reads "Saved" while the client's intake — the source of every generated document — never persisted. Frontend twin of backend **E**.

**Six new systemic themes (carry the encompassing fixes):**

- **T13 — Frontend over-reports success / trusts the callable blindly.** The UI declares success on mere promise-resolution (or a `docType`+timestamp listener match) without checking `result.status`/`result.success`. Cluster: CR (SingleDocumentGenerator), CS (FlexDocumentGenerator), CT (GenerateDocumentsButton all-selected), CU (ESignatureDialog), DG (ChargePaymentDialog capture status), DW (SettingsPage fake connection test). Directly compounds backend **E / BU / BP** — a doc saved with `status:'error'`, an unsent e-sign, or an authorized-not-captured charge all render the green "done" screen.
- **T14 — Pervasive convention drift (CLAUDE.md violations).** Forms use raw `useState` instead of react-hook-form + Zod across nearly every dialog/page (CM, CN, EN…); components call `httpsCallable` directly and/or write Firestore raw instead of going through `src/services/*` (EM + sites in payments, settings, AI widget, calendar, KB). Individually minor; collectively the validation/data-access contract isn't enforced anywhere.
- **T15 — Composite `.name` read where only name-parts were written.** Review screen, printable questionnaire, fiduciary blocks, and the grandchildren "parent" dropdown all read a single `.name` field, but only `PersonPicker` writes it — manual entry and the questionnaire store `firstName/middleName/lastName`. Result: blank/"Not yet named"/empty-dropdown on the confirmation and print views (CK, CJ). Same root as backend **AN**; needs one shared `resolveDisplayName(parts)` at every read site.
- **T16 — Divergent client-creation shapes vs the data model.** NewClientPage, DashboardPage's inline create, and BulkImportModal each write a **different** client object, several diverging from `types/index.ts` (top-level `packageType`/`questionnaire`/`firstName` instead of `packageDetails.packageType` / `questionnaireProgress` / `personalInfo`; missing `isActive`/`isArchived`/`assignedAttorneyId`). New clients carry orphaned fields and never show a package (DY, + BulkImport/Dashboard).
- **T17 — Capped or unbounded list reads drive UI truth.** DashboardPage derives all stats, queues, AND search from a `limit(50)` client read (wrong counts + older clients unsearchable, DX); PaymentsPage totals from `limit(500)` (DZ); ClientListPage / DashboardPage doc-group / NameSplitsReview do unbounded scans (EA, EB). Caps silently lie; unbounded reads are cost/latency (ties to the project's OOM history).
- **T18 — UI-only access gates + secrets in the browser (defense-in-depth; B0-adjacent).** Firm provider keys + Google OAuth access/refresh tokens ship to the client in plaintext on the firm doc (DU, masking is cosmetic); TeamTab lets the caller pick `role:'admin'`/any capability and posts it verbatim, gated only client-side (DV); printable & client-portal routes lack auth/ownership guards (EG, EH); `usePermissions` grants users/billing/settings to any attorney *or* paralegal (EI). Correctness rests entirely on `firestore.rules` + the deferred B0 server-side fixes (AP/AQ/AR/AS). The frontend cannot fix these — but it shouldn't *present* them as if safe.

**Recommended Round-4 fix batches (encompassing):**
- **B13 (T13, 🔴/🟠) — Truth-in-status on the frontend.** Derive every "saved/sent/charged" UI state from `result.status`/`result.success` (and filter fallback snapshots by `status !== 'error'`). One helper convention across CR/CS/CT/CU/DG/DW. Pairs with backend B2.
- **B14 (T16+DF, 🔴/🟠) — One canonical client-write path.** A single `createClient()` that writes the `types/index.ts` shape; fixes DY + BulkImport/Dashboard drift. Fold in the DF calendar-field correction (write+read agree on `clientId`).
- **B15 (T15, 🟠) — Shared `resolveDisplayName`.** Compose name from parts at every read site (CK, CJ, review/print, fiduciary blocks).
- **B16 (data integrity, 🔴/🟠) — Questionnaire save reliability.** Wrap the primary `setDoc` in try/catch + the existing retry + `SET_ERROR` (CH); make `canProceed` object/array-aware so required address/repeater steps actually enforce (CI).
- **B17 (T17, 🟠) — Aggregation/pagination.** Replace cap-driven stats/search/totals with count/aggregation queries; single-`getDoc` the per-row NameSplits lookups (DX/DZ/EA/EB).
- **B18 (T14/T18) — Convention + guards.** Route components through the service layer, migrate forms to RHF+Zod, add route ownership guards; server-side secret/role enforcement stays owned by **B0**.

> Standalone quick wins (not worth a batch): DH (debug `alert()` in prod), DD (debug `console.log` leaking ids), CV (regenerate stale-content data-loss — `setContentLoaded(false)` on regen), EE (`window.__firebase` exposure in prod).

**Verified NON-findings (checked, no bug):** `useFirestore.useCollection` constraint key — `JSON.stringify` of a Firestore `QueryConstraint` **does** capture `_field`/`_op`/`_value` (confirmed by running the SDK: changing a `where` value yields a different key), so value changes re-subscribe correctly; the "stale query" concern is wrong. `CurrencyField` stores a `number` (not a formatted string) → no `$NaN` from this path. Name validation (`sanitizeNameField`) correct. `GlobalAiWidget` renders AI replies as a text node (no XSS). DOMPurify (`sanitizeHtml`) is correctly applied at **every** `dangerouslySetInnerHTML` in the documents/editor slice and the KB previews. Activity-logger `clientId` invariant honored at every in-scope call site. Date handling in DeadlinesCard/TasksList correctly avoids the UTC off-by-one.

**Running tally across rounds 1-4:** ~126 findings. 🔴 ×9 (E, AF, AP, AQ, AZ/BA, BN, BT + frontend DF, CH). **Audit-only sweep COMPLETE — all four rounds done; no fixes applied.**

## Round 4 findings

### Client-facing pages + questionnaire intake (`pages/client`, `components/questionnaire`, `contexts/QuestionnaireContext`)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| CH | 🔴 | **fixed (#51)** | **Questionnaire intake silently fails to save while UI shows "Saved."** `QuestionnaireContext.performSave` (L347-464) wraps the primary client-data `setDoc` in `try…finally` with **no `catch`**; only the secondary `questionnaireProgress` `updateDoc` is in the retry loop / dispatches `SET_ERROR`. Autosave fires it as `void performSave(...)` → a rejected primary write is an unhandled rejection, no retry, no error surfaced, `isSaving` resets. The data source for every generated document is lost with a "Saved" indicator. Frontend twin of **E**. Fix: wrap the whole body in try/catch + `SET_ERROR`, put the primary `setDoc` in the retry loop. **T13.** |
| CI | 🟠 | confirmed | **Required address/repeater steps not enforced.** `canProceed` (L583-595) checks `val != null && val !== ''`. An `address` field's `name` is the object path (`personalInfo`) → always a non-null object → passes with blank street/city/zip; a `required` `repeater` (e.g. children) has `val = []` → `[] != null && [] !== ''` is true → "Yes I have children" advances with zero entered. Fix: object/array-aware required check (non-empty subfields / `length > 0`). **B16.** |
| CJ | 🟠 | confirmed | **Grandchildren "Parent's Name" dropdown is always empty.** Field `optionsFrom:{source:'children',labelField:'name',valueField:'name'}` (questionnaire.ts L876-880), but children store `firstName/middleName/lastName` with no composite `name`; RepeaterField (L127-139) does `.filter(item => item[labelField])` → all filtered out → no options ever. Fix: point at a real field or compose the name in the resolver. **T15.** |
| CK | 🟠 | confirmed | **Manually-typed fiduciaries/guardians show blank in review + print.** QuestionnaireComplete (L237-251) and PrintableQuestionnaire (L171-188) read `executor.primary.name` / `powerOfAttorney.agent.name` / `healthcareProxy.agent.name`, but only `PersonPicker` writes `.name`; direct entry saves `firstName/lastName` → review reports "Not yet named", print shows empty Full Name. Misleads the client at the confirmation step. Fix: compose from parts (shared `resolveDisplayName`). **T15.** |
| CL | 🟡 | confirmed | **Review accordion renders objects as `[object Object]`.** For address-type fields (`name`=`personalInfo`/`spouseInfo`/`guardianPrimary`) QuestionnaireComplete (L157-177) falls through to `String(rawVal)` → "Home Address: [object Object]". Fix: detect objects and format the address. |
| CM | 🟡 | confirmed | **No Zod/validation layer.** `FieldConfig.validation` (pattern/min/max/minLength) and the `validateEmail`/`validatePhone` helpers are never invoked (StepRenderer / questionnaire.ts L72-80); validation is presence-only. Diverges from the RHF+Zod rule on the data source for every legal doc. **T14.** |
| CN | 🟡 | confirmed | **AddressField inputs are uncontrolled** (`defaultValue` + manual `ref.value` sync, L113-258) — documented Google-Places workaround, but bypasses controlled-input validation and risks edits not repainting on external updates (resume-from-Firestore, "Same as my address"). Confirm via browser test. **T14.** |
| CO | 🟡 | confirmed | QuestionnaireShell section-transition banner (L404-415/258-267) sets `showSectionBanner=true` on change but never resets, and `SectionBanner`'s internal `show` is never re-set on subsequent `section.id` changes → banner appears once then never again. Cosmetic. |
| CP | ⚪ | confirmed | `numberOfChildren` is loaded + persisted (`?? null`) but never derived from `children.length` → stays null. Low impact unless a generator reads it instead of `children.length`. |
| CQ | ⚪ | confirmed | QuestionnaireUploader client `maxSize` hard-coded 50 MB + accepts images, but CLAUDE.md says `uploads/` caps at 20 MB → if client uploads route there, files pass the client check then fail at storage rules with a generic error. Confirm destination in `storageService.uploadClientFile`. |

### Document generation / vault / export / editor (`components/documents`, `components/editor`)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| CR | 🟠 | confirmed | **SingleDocumentGenerator over-reports failed gen as "saved to the Document Vault."** `markSuccess(result.title)` (L127-134) ignores `result.status`, and the fallback Firestore listener (L136-148/185) fires `markSuccess` on any doc matching `docType` with `updatedAt>=startTime` — including one written `status:'error'` (backend E). Fix: gate on `result.status !== 'error'` and filter the snapshot by status. **T13.** |
| CS | 🟠 | confirmed | **FlexDocumentGenerator fallback hardcodes `success:true`** (L226-237) for any matching doc regardless of real `status`, then shows "added to the Document Vault." Fix: read `data.status`, route to the error phase on `error`. **T13.** |
| CT | 🟠 | confirmed | **GenerateDocumentsButton all-selected path no per-doc error check.** Success dialog shows `result.documentsGenerated` and asserts "All documents have been drafted" with no `status:'error'` filter (the subset path *does* check, L240-252/635-640). Over-reports given backend E. Fix: count non-error results like the subset branch. **T13.** |
| CU | 🟠 | confirmed | **ESignatureDialog reports "Sent Successfully" without checking the result** (L43-50) — never inspects `res.success`/`signatureRequestId`. Compounds backend **BU** (e-sign is a no-op): a confident false delivery claim for a legal doc. Fix: gate `setSuccess` on `res.success===true && res.signatureRequestId`. **T13.** |
| CV | 🟠 | confirmed | **Regenerate → stale content → data loss.** DocumentEditor's content-load effect early-returns when `contentLoaded` (L242), but `handleRegenerate` (L378-413) never resets it → editor keeps OLD HTML after regen; next keystroke autosaves the stale HTML over the regenerated draft. Fix: `setContentLoaded(false)` (or re-`setContent`) when the subscription delivers new `editorContent` post-regen. |
| CW | 🟠 | confirmed | **CommentsPanel reply race + permanently-null timestamps.** `handleAddReply` (L398-414) read-modify-writes the in-memory `replies` array → concurrent replies clobber (lost reply) in this real-time multi-user panel; `createdAt:null` inside an array can't be replaced by `serverTimestamp()`, so every reply shows "just now" forever. Fix: `arrayUnion`/subcollection + `Timestamp.now()`. |
| CX | 🟡 | confirmed | **AttorneyReviewGate audit record missing the timestamp it advertises.** Disclaimer (L8/211-213) promises "name and timestamp," but `updates` (L137-151) writes only `reviewedBy`/`reviewNotes`/`status`, and status is `'review'` not approved/final despite "Approve for export." Fix: add `reviewedAt: serverTimestamp()`; confirm terminal status. |
| CY | 🟡 | confirmed | **Stale-closure in TipTap `onUpdate`** (DocumentEditor L158-347) captures first-render `saveVersion` closing over `document?.currentVersion` → periodic version snapshots (every 15 saves) can be numbered off a stale `currentVersion` (duplicate `versionNumber`). Fix: store the live callback / read `currentVersion` from a ref. |
| CZ | 🟡 | fixed | DocumentEditor fed AI/DOCX HTML to TipTap `setContent` without `sanitizeHtml`, unlike every read-only surface. **Fixed (2026-06-30):** both `setContent` call sites in `DocumentEditor.tsx` (initial load + `handleRestoreVersion`) now wrap content in `sanitizeHtml()`; the restore path also autosaves the sanitized content so editor state and the saved doc stay consistent. |
| DA | 🟡 | confirmed | DocumentVault "Regenerate with updated data" action is dead — `RowActions` renders it only when `onRegenerate` is truthy, but the main table never passes it (L206-332/840-857); the `isStale` UI is unreachable. Wire it or remove. |
| DB | 🟡 | confirmed | VersionHistory `simpleDiff` (L145-181) is set-based on `'. '`-split sentences: dedupes identical sentences, loses order, renders all removals before additions → can misrepresent edits to legal text (the correct UI is DocumentDiffDialog). |
| DC | 🟡 | confirmed | EditorToolbar `handleLinkInsert` (L337-360) interpolates user `url`/`text` into an `<a href="${url}">${text}</a>` string for `insertContent`. TipTap's allowlist blocks `javascript:` and parses (not executes) → self-XSS-only/low, but fragile. Prefer `setLink({href})`. |
| DD | ⚪ | confirmed | DocumentPreviewDialog debug `console.log('[…DEBUG]', …)` (L275-285) leaks docId/storagePath/clientId on every open. Remove. |
| DE | ⚪ | confirmed | `lib/sanitize.ts` (L44-57) permits `a target` without forcing `rel="noopener noreferrer"` (reverse-tabnabbing) and `img src` with no URI restriction; DOMPurify still blocks `javascript:`/handlers, so minor. Add `rel=noopener` via hook; consider restricting `img`. |

### Dashboard / payments / clients (`components/dashboard`, `components/payments`, `components/clients`)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| DF | 🔴 | confirmed | **Per-client calendar permanently empty.** CalendarTab queries `where('relatedClientId','==',clientId)` (L1305) but events are created with field `clientId` (L1145-1146) and `CalendarEvent` (types/index.ts L1061) has no `relatedClientId`. Every client-scoped CalendarTab returns zero events, including ones just created. Fix: query `clientId`. (Fold the write/read field agreement into B14.) |
| DG | 🟠 | confirmed | **ChargePaymentDialog shows "Payment Processed!" for any `success:true`, ignoring capture status.** `ChargeResult.status` (declared L54) is never inspected (L482) → an authorized-not-captured charge renders the green success screen. Frontend twin of **BP**. Fix: branch on `chargeResult.status` (success only for captured/completed). **T13.** |
| DH | 🟠 | confirmed | **Leftover debug `alert()` in prod.** CalendarTab GCal-sync catch blocks (L857, L1175) call `alert('GCAL ERROR: '+…)`; since GCal `invalid_grant` is a recurring condition, users get a blocking browser alert on every create/edit. Fix: remove the `alert`, keep `console.error`. |
| DI | 🟠 | confirmed | **Naive CSV parsing corrupts bulk-imported clients.** BulkImportModal `lines[i].split(',')` (L98/108) splits every comma → any quoted field with a comma (`"Smith, Jr."`, addresses) shifts all later columns into the wrong fields. Fix: quote-aware CSV parse. |
| DJ | 🟠 | confirmed | **"New Clients (6mo)" undercounts across the year boundary.** AnalyticsWidgets (L161-167) reparses each bucket label with the *current* year (`new Date(\`1 ${m.label} ${now.getFullYear()}\`)`) → a Dec-2025 client viewed in Jan 2026 matches "Dec 2026" → never counted. Fix: bucket by actual year+month, not a reparsed label. |
| DK | 🟠 | confirmed | **Empty catch hides bulk-import failures.** BulkImportModal (L264) `catch { results.set(i,{imported:false,error:'Failed to create'}) }` discards the real Firestore error with no log → undebuggable. Fix: `console.error` the caught error. |
| DL | 🟡 | confirmed | PaymentsTab SendRequestDialog (L583) calls `handleClose()` right after `setResultUrl(...)`; `handleClose`→`resetForm` nulls `resultUrl` → the "Open payment page" link (L655) is dead code (standalone SendPaymentDialog does it right). Fix: don't auto-close on success. |
| DM | 🟡 | fixed | DashboardPayments (and PaymentsPage) rows rendered `formatCents(p.amount)` while the summary cards sum `amountPaid`/`balanceDue` → a partially-paid record didn't reconcile. **Fixed (2026-06-30):** rows now use `statusAmountCents(p)` (paid→`amountPaid`, pending/overdue→`balanceDue`, else→`amount`), mirroring the summary logic. No change for fully-paid/unpaid records; only partial-payment rows are corrected. |
| DN | 🟡 | confirmed | NotesTab (L734) prints `note.createdBy` (raw Firebase UID) as the author. Fix: resolve UID→display name. |
| DO | 🟡 | confirmed | NotesTab `handleDone` (L352-369) does `setDoc(...).catch(console.error)` un-awaited then immediately `onClose()` → a slow/failed final note write is silently lost. Fix: await + surface errors. |
| DP | 🟡 | confirmed | BulkImportModal (L136) sets any unknown `packageType` to `''` with no row warning → client imports without the specified package. Fix: flag the row. |
| DQ | 🟡 | confirmed | BulkImportModal (L237) does no email-format validation; emails feed client-link/auth matching → a malformed/blank email imports silently and breaks linking. Fix: validate + mark bad rows. |
| DR | 🟡 | confirmed | BulkImportModal `results` map (L223/262) is populated but never applied back to `rows` → the preview's per-row imported/error status never updates. Wire it or remove. |
| DS | 🟡 | confirmed | ChargePaymentDialog billing-ZIP prefill (L251-254, dep `[defaultZip]`, guard `!billingZip`) won't refill on client switch if a ZIP was typed → wrong-ZIP-on-AVS risk. |
| DT | ⚪ | confirmed | `estimatedFee`/`retainerPaid`/`balanceDue` (types/index.ts L784-786) lack the "in cents" comment `Payment.amount` has; every consumer divides by 100 so it's *consistently* cents today, but the un-annotated type is a latent AX/BR ambiguity. Add `// in cents`. |

### Admin pages / settings / team (`pages/admin`, `components/settings`)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| DU | 🟠 | confirmed | **Live firm API keys + OAuth tokens shipped to the browser in plaintext.** `useDocument(firmPath)` pulls the whole firm doc; SettingsHelpers `ApiKeyField storedKey=` binds `openAiApiKey`/`anthropicApiKey`/`geminiApiKey`/`perplexityApiKey`/`sendGridApiKey`/lawpay/assemblyai/levitate/courtlistener **plus Google Calendar/Drive access+refresh tokens**. Masking is display-only; the raw values are in the network response + client memory, readable by any in-firm staff (firestore.rules permit it). Frontend surface of **AR / B0**. Fix (server-side): expose only `hasKey`+last-4; keep secrets off the client-readable doc. |
| DV | 🟠 | confirmed | **Privilege-escalation UI surface.** TeamTab create/edit dialogs (L97-256) let the caller pick `role:'admin'` and any capability, then send `role`/`capabilities` verbatim to `createFirmUser`/`updateUserCapabilities`; the only gate is the client-side `canManageUsers` check (bypassable via devtools). The exact UI vector for backend **AP/AQ**. Self-edit of own capabilities is also possible. Fix is server-side (B0); optionally hide `admin`/`manage_users` for non-admins. |
| DW | 🟠 | confirmed | **Fake "connection test" success.** SettingsPage `handleTestConnection` (L552-568) does a real check only for SendGrid; for LawPay (and others) it `await`s an 800 ms `setTimeout` then toasts "…connection test successful." Success shown for an operation that never ran. Fix: real verification callable or remove the button. **T13.** |
| DX | 🟠 | confirmed | **Dashboard stats/queues/search silently capped at 50 clients.** `allClients` uses `limit(50)` (L272-275); Active-Clients/Questionnaires-Pending counts, Ready-to-Draft, Awaiting-Review, Upcoming-Deadlines (L312-431) and the client search (L416-431) all derive from only the 50 most-recent clients → wrong numbers + older clients return "No clients match." Fix: count/aggregation queries (or server-side search). **T17.** |
| DY | 🟠 | confirmed | **NewClientPage writes wrong data-model fields.** Persists top-level `packageType:null` + `questionnaire:{status}` (L149-165), but the model is `packageDetails.packageType` + `questionnaireProgress` (types/index.ts L781/860-863); also omits `isActive`/`isArchived`/`assignedAttorneyId`. DashboardPage's inline create (L457-467) writes a *third* shape (top-level `firstName/lastName`+`personalInfo`+`status:'active'`). New clients carry orphaned fields, never show a package. Fix: one canonical `createClient`. **T16.** |
| DZ | 🟡 | partial | PaymentsPage (`limit(500)`) and DashboardPayments (`limit(200)`) summary cards silently undercount beyond the cap. **Partial fix (2026-06-30):** the undercount is no longer *silent* — both surfaces now show "Showing the most recent N payments — totals may exclude older records" when the row count hits the cap. **Still OPEN (T17, needs sign-off):** the proper fix is server-side `sum()` aggregation, which requires composite indexes (firmId+status) and a manual `firebase deploy --only firestore:indexes` (not auto-deployed in CI) → Never-Break, deferred for explicit approval. |
| EA | 🟡 | confirmed | Unbounded reads: ClientListPage subscribes to *all* clients (`orderBy('updatedAt')`, no limit, L160-163); DashboardPage runs an unbounded `documents` collection-group listener (L279-282). Cost/latency scale with firm size. Paginate/cap. **T17.** |
| EB | 🟡 | confirmed | NameSplitsReview `applyToClient` (L208) calls `getDocs(collection(.../clients))` per approve/skip just to find one doc; initial load (L152) is also an unbounded scan. Fix: `getDoc` by id. (Admin migration tool → 🟡.) **T17.** |
| EC | ⚪ | confirmed | SettingsPage MFA card copy (L2104-2106) tells the user to "activate it by uncommenting the Firebase MFA calls," but `handleStartMfaEnrollment` (L737-755) already calls real TOTP MFA. Stale/misleading copy. |
| ED | ⚪ | confirmed | DashboardPage: `Record<string, any>` note shape with an eslint-disable (L499-500) — bans-`any` violation; `toggleExpanded` (L257) swallows the preference-write error with `.catch(console.error)` (no user feedback). |

### Auth / state / routing / services / config (`contexts`, `hooks`, `services`, `config`, `App.tsx`)

| ID | Sev | Status | Summary |
|----|-----|--------|---------|
| EE | 🟠 | confirmed | **Authed Firebase handles exposed on `window`.** `firebase.ts` (L113-116) attaches `auth`/`db`/`storage`/`functions` to `window.__firebase` (via `as any`) on every load → any injected script/extension/XSS gets the signed-in user's Firestore/Functions handles for exfiltration. Fix: gate behind `import.meta.env.DEV` or remove in prod. |
| EF | 🟠 | confirmed | **Over-broad Google Drive OAuth scope.** SettingsHelpers `GoogleDriveLoginButton` (L70-75) requests full `auth/drive` (read/write/delete ALL Drive files) while the comment claims `drive.file`. Use `drive.file` (per-file) unless full Drive is genuinely required. |
| EG | 🟡 | confirmed | **Printable routes have no auth guard.** App.tsx (L218-219) `/questionnaire/print` + `/questionnaire/:firmId/:clientId/print` render with no layout/`useRequireAuth` wrapper — only firestore.rules stand between an unauth visitor and the page shell. Fix: wrap in `ClientLayout`/a guard. **T18.** |
| EH | 🟡 | confirmed | **ClientLayout has no firm/client ownership check** (L24-38) — only redirects when `!user`; any authed user can mount `/portal/{otherFirmId}/{otherClientId}` (data load gated only by rules). Verify `userProfile.firmId===firmId` + ownership before render. **T18.** |
| EI | 🟡 | confirmed | `usePermissions` (L29-33) resolves `canManageUsers`/`canManageBilling`/`canManageFirmSettings` to `true` for any attorney *or* paralegal by role (the `|| hasCapability` never restricts) → paralegals get full users/billing/settings UI. UI-only, but over-exposes admin surfaces. Frontend twin of **AS**. **T18.** |
| EJ | 🟡 | confirmed | LoginPage client routing (L126-129) matches a client record by `personalInfo.email == user.email` with **no `email_verified`** check → an unverified-but-matching email is routed into that client's questionnaire (read is rules-gated, but routing shouldn't). Frontend twin of **BL**. |
| EK | 🟡 | confirmed | LoginPage `firmId` falls back to hard-coded `'elias-counsel'` in both the routing effect (L102) and the sign-up `linkClient` call (L202) → latent multi-tenancy bug: a 2nd firm's new clients silently link to the wrong firm. |
| EL | 🟡 | confirmed | recommendation-engine (L61-67) reads `wantsMedicaidPlanning`/`hasBlendedFamily` from `data.specialConsiderations.*` via `as unknown` casts, but `QuestionnaireData` has no such field (it's on `Client`, types L857) and the questionnaire never collects it; PackageSelector (L248) passes questionnaire `data` → both flags always false → the Medicaid (+3 fortress) and blended-family (+2 guardian) scoring factors are dead. |
| EM | 🟡 | confirmed | **`httpsCallable` called directly in components + raw Firestore writes**, bypassing the mandated service layer. Sites: SettingsPage (exchangeGoogleAuthCode/connectGoogleDrive L625/676), CalendarPage (triggerFirmCalendarSync L28), LoginPage (linkClient/getFirmBranding L106/153/203), payment dialogs (ChargePaymentDialog L100, SendPaymentDialog L27, PaymentsTab L196), GlobalAiWidget (listAiConversations/saveMessageAsNote/chatAi + `setDoc`/`getDocs` L188/252/298/347-375/450), TemplatePreviewDialog (enhanceTemplate L299), and raw-Firestore in BulkImportModal/DeadlinesCard/NotesTab/TasksList. **T14.** |
| EN | 🟡 | confirmed | **Forms use `useState` not react-hook-form + Zod** across NewClientPage, NameSplitsReview, SettingsPage (incl. a raw `<select>`), QuestionnaireRegisterPage, all payment/calendar/deadline/bulk-import dialogs, and the questionnaire flow. Pervasive convention violation on the data sources for legal docs. **T14.** |
| EO | ⚪ | confirmed | Multiple `any` (lint-disabled) violating the no-`any` rule: knowledge-base-service (`res.data as any` ×5, createdAt/updatedAt), document-service self-referential `ReturnType<typeof this.…>` circular typing (L467/489/509), useGooglePlacesAutocomplete (`Window.google:any`), TemplatePreviewDialog (`result.data as any` L308). Type the known response shapes. |
| EP | ⚪ | confirmed | AuthContext `getCustomClaims` calls `getIdTokenResult(true)` (force refresh) inside `onAuthStateChanged` (L103) → unnecessary network round-trip on every ~hourly token refresh + tab focus (force-refresh only needed right after `linkClient`). Also the file header (L1-15) documents an inactivity session-timeout that lives in AppLayout, not here (stale comment). |
| EQ | ⚪ | confirmed | QuestionnaireRegisterPage (L112-168) calls `signInAnonymously` *before* `registerClientFromLink`; if the callable fails, an anonymous auth session is left signed in with no cleanup (ClientLayout treats anon as authed) → can confuse subsequent loads. |
| ER | ⚪ | confirmed | firebase.ts (L67-78) wraps `initializeFirestore(persistentLocalCache)` in try/catch assuming a synchronous throw, but IndexedDB/persistence failures surface asynchronously → the `memoryLocalCache` fallback likely never triggers on the real failure path. |
| ES | ⚪ | confirmed | Two files named `sanitize.ts` (`lib/` = DOMPurify `sanitizeHtml`; `utils/` = input/name/prompt-injection) — naming collision invites wrong imports. `utils/sanitize.sanitizeInput` is a regex tag-stripper (weaker than DOMPurify, safe only because it re-escapes to text) — never use it for HTML rendered via `dangerouslySetInnerHTML`. |
| ET | ⚪ | confirmed | Minor: `settings-utils.maskApiKey` returns a trailing space and `''` for keys <4 chars (short key shows blank); SettingsHelpers `ColorPickerRow` builds element ids with embedded spaces (`cp - ${label}`) and clicks via `getElementById` (fragile, collides on duplicate labels); GlobalAiWidget `resumeConversation` casts `conv.mode as 'chat'|'draft'` but the union includes `'research'` (lossy). |
